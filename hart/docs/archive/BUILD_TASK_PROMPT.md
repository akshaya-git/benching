# agedIn — scheduled build task (spec + full instructions)

## Schedule spec (for the new chat to use)
- **Type:** one-shot scheduled task (cron) — NOT idle-time (idle queue is disabled on this account)
- **When:** tomorrow, 8:00 AM local (PDT) — cron `0 8 20 9 *`, recurring=false, maxRuns=1
- **Title:** `agedIn full MVP build + 3-framework validation (8 AM PDT)`
- **One-line request to paste into a NEW chat:**
  > Schedule a one-shot cron task for tomorrow 8:00 AM (local/PDT) with maxRuns 1, titled "agedIn full MVP build + 3-framework validation (8 AM PDT)", whose prompt is the full text under "TASK PROMPT" in /Users/darthvader/Documents/agedIn/BUILD_TASK_PROMPT.md — read that file and use its TASK PROMPT section verbatim as the task's prompt.

---

## TASK PROMPT (use verbatim)

Build the complete working MVP of the "agedIn" agentic harness in /Users/darthvader/Documents/agedIn/, implementing /Users/darthvader/Documents/agedIn/DESIGN.md (read it fully first, including §4.4 run lifecycle and the §6 interface model; also §5.2 for the benchtest result-row schema). The owner will test it independently afterward — it must actually run end-to-end against all three Apple Silicon frameworks. Work autonomously; if you must deviate from the design, record why in DEVIATIONS.md rather than stalling.

WHAT TO BUILD — the full MVP per the design, as a single-file Python stdlib-only CLI `agedIn.py` (per DESIGN.md §7):
1. CLI per §6: --base-url, --model, --task / --task-file, --workdir, --resume [RUN_ID], --fresh, --max-steps (default 40), --ctx-tokens, --per-call-tokens (default 8192), --time-budget (default 3600), --effort, --metrics-out, --quiet; plus the `agedIn.py runs` listing subcommand; exit codes 0 pass / 1 task failed / 2 infra error / 130 aborted.
2. Blackboard per §2.2: <workdir>/.agedIn/state.json with atomic write-through (temp + os.replace), append-only events.jsonl journal, per-run directory isolation, LOCK marker with stale-lock pid detection.
3. JSON-action protocol per §2.3 — the single envelope for all agents: strict schema, graduated extract_json parse (strict → first {…} block → fence-stripped, same strategy as benchtest's HTML extractor), repair retries (max 2) with error observations, actions write_file / run_shell / done; run_shell hardening: deny-patterns (rm -rf ~, sudo, curl|sh), cwd jail to workdir, 60s timeout, output truncated.
4. All six agents per §3: Connection (probe /v1/models, adopt served model id when exactly one served and configured id absent — MTPLX normalizes names; streaming chat; TTFT counts first delta of content OR reasoning_content; graceful retry without stream_options on HTTP 400), Planning (3–7 steps with verifiable outputs; one repair retry; fallback single-step plan), Builder (bounded actions per step — 8; writes via write_file; marks steps done), Validator (deterministic first: for .html run the QA gate ported from benchtest — node --check on inline JS + weighted checklist with the 90% usable threshold and syntax-error cap at 25%; for .py run py_compile; then one model review call with verdict JSON; bounded validate→build fix-loop, max 2 round-trips, then proceed with failures documented), Integrator (advisory holistic review + writes README.md via the same protocol), Summary (human report to stdout + REPORT.md, last stdout line always `AGEDIN_RESULT {…}` compact JSON).
5. Resilience per §4: checkpoint after every agent transition/model call/file write; --resume loads state.json and enters the first pipeline stage not done; SIGTERM/SIGINT handler writes state and exits 130; context compaction at 60% of ctx budget (observation-history only; file contents always re-read from disk, never accumulated); loop ladder — identical-action hash 3× nudge / 4× re-plan / 5× fail run; workspace no-progress hash; all budgets enforced including whole-run wall clock.
6. Metrics per §5: every model call appends {ts, agent, call, ttft, pp, tgs, tps, prompt_tokens, completion_tokens, wall, finish, model} to .agedIn/metrics.jsonl (streaming measurement identical to benchtest call_chat); final agedIn-metrics.json with per-agent rollups and a result row whose keys match benchtest runs/*.json exactly (framework, model, harness:"agedIn", task, status, latency, tokens, tps, output_url, qa_func, qa_qual, usable, truncated, ttft, tgs, pp, prompt_tokens, error).
7. README.md: install/run, one example per framework, resume/compaction/loop-guard explanation, metrics description, honest limitations.

THEN VALIDATE END-TO-END against ALL THREE frameworks — real servers, not mocks. Exact start commands are in /Users/darthvader/Documents/benchtest/server.py FRAMEWORKS: OMLX :7001 (`omlx serve --port 7001`, model scottlowry--Qwen3.8-27B-oQ6e-mtp), MTPLX :7002 (start via the mtplx serve command there; the server serves the model as mtplx-qwen38-27b-optimized-quality — let Connection adopt it), MLX-LM :7003 (AutomatosX/AX-Qwen3.8-27B-MLX-AXQ-6bit-MTP via the mlx_lm.server command there; loads weights lazily — first call is slow). For EACH framework: start it (nohup, log under /tmp), poll /v1/models until healthy, send a trivial warmup completion, then run agedIn end-to-end with --task "Write hello.html: a single HTML page with an <h1>hello</h1> and a button that shows an alert." --per-call-tokens 4096 --time-budget 600. Assert: artifact hello.html exists in the workdir, state.json pipeline reaches summary done, metrics.jsonl contains calls with ttft/tgs/tps populated, stdout ends with a parseable AGEDIN_RESULT line. Additionally verify ONE resilience path live (any one framework): start a run, send SIGTERM mid-run (state must persist, exit 130), then --resume must complete the run. After each framework, kill exactly the server you started (SIGTERM its process group; NEVER kill a server you did not start; verify ports before/after). Record per-framework PASS/FAIL + evidence (latencies, artifact path, metrics excerpt, resume outcome) in VALIDATION.md.

CONSTRAINTS: Do NOT modify anything under /Users/darthvader/Documents/benchtest — the owner integrates agedIn as the 5th harness personally. No new Python dependencies. If a framework fails, capture its actual error and continue to the next; all three must be attempted. Leave no background processes; clean your /tmp scratch files. Bound each framework session to ~10 minutes.

FINISH with a summary of: files built + LOC, per-framework validation table (PASS/FAIL + one-line evidence), the resume test outcome, known gaps vs DESIGN.md, and the exact commands the owner should run to test agedIn independently.
